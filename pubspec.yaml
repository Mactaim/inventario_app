name: inventario_app
description: "Aplicación de inventario para Retablo's Café"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path: ^1.8.3
  flutter_lints: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1   # 👈 añadido para personalizar icono

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"   # 👈 tu logo aquí
  adaptive_icon_background: "#6B4226"      # marrón café
  adaptive_icon_foreground: "assets/icon/app_icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/icon/app_icon.png   # 👈 asegúrate de tener tu logo en esta ruta